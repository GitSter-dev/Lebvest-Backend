services:
  postgres:
    image: 'postgres:16'
    container_name: postgres
    environment:
      - 'POSTGRES_DB=lebvest'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=admin'
    ports:
      - '5460:5432'
    volumes:
      - pg_data:/var/lib/postgresql

volumes:
  pg_data:
